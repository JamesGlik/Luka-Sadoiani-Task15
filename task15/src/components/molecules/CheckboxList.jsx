import React from 'react';
import CheckboxItem from '../atoms/CheckboxItem';

const CheckboxList = () => (
  <div>
    <CheckboxItem label="Product discovery and building what matters" />
    <CheckboxItem label="Measuring to ensure updates are a success" />
    <CheckboxItem label="And much more!" />
  </div>
);

export default CheckboxList;